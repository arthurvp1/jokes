const n1 = 10;
const n2 = 20;
const soma = n1 + n2;
const subtracao = n1 - n2;
const multiplicacao = n1 * n2;
const divisao = n1 / n2;



console.log(`A soma de ${n1} + ${n2} = ${soma}`);

console.log(`A subtração de ${n1} - ${n2} = ${subtracao}`);

console.log(`A multiplicação de ${n1} * ${n2} = ${multiplicacao}`);

console.log(`A divisão de ${n1} / ${n2} = ${divisao}`);
